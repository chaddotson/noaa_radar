py-radar
========

Tools for downloading and overlaying radar images from the Radar Ridge radar site.

For an map of radar sites see, the included noaa_radar_coverage_map.jpg. On the map,
Radar site names start with a K, just remove it and you have the name string of the site
to request.  For example, 'KHTX' becomes 'HTX'.
